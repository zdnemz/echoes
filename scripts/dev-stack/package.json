{
  "name": "echoes-dev-stack",
  "private": true,
  "version": "1.0.0",
  "description": "Local Supabase-compatible stack (Postgres + PostgREST + GoTrue + gateway) for running Echoes without cloud credentials. See scripts/dev-stack/README.md.",
  "dependencies": {
    "pg": "8.13.1"
  }
}
